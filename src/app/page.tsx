import BrainApp from "@/components/BrainApp";

/** Static shell; the graph is fetched in the browser (public copy or GitHub for the admin). */
export default function Home() {
  return <BrainApp />;
}
